// @ts-check

/* eslint-disable  @typescript-eslint/no-var-requires */

/** @typedef { import('../types').Rules } Rules */

const { off, warn, error } = require('../types');

module.exports = { off, warn, error };
